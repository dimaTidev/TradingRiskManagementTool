/**
 * @typedef {Object} PlatformAPI
 * @prop {function} createClient
 * @prop {function} deleteClient
 * @prop {function} submitOrder
 * @prop {(ticker: string) => number} getTickerPricing
 * @prop {function} getAccountInfo
 */

export const Types = {}